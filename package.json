{
  "name": "phantombotai",
  "private": true,

  "workspaces": [
    "apps/*",
    "packages/*"
  ],

  "scripts": {
    "dev:dashboard": "npm --workspace apps/merchant-dashboard run dev",

    "replay:reset": "tsx apps/replay-controller/src/index.ts",

    "dev:ingestion": "npm --workspace apps/ingestion-api run dev",

    "dev:worker": "npm --workspace apps/worker-runtime run dev",

    "worker:session": "tsx apps/session-projection-worker/src/index.ts",

    "db:migrate:projection": "docker exec -i phantom-postgres psql -U postgres -d phantom < infrastructure/postgres/migrations/002_projection_runtime.sql",

    "infra:up": "docker compose -f infrastructure/docker/docker-compose.yml up -d",

    "infra:down": "docker compose -f infrastructure/docker/docker-compose.yml down",

    "infra:logs": "docker compose -f infrastructure/docker/docker-compose.yml logs -f"
  },

  "dependencies": {
    "dotenv": "^16.4.5",

    "postgres": "^3.4.4",

    "redis": "^4.6.13",

    "zod": "^3.23.8"
  },

  "devDependencies": {
    "@types/node": "^22.5.4",

    "tsx": "^4.19.1",

    "typescript": "^5.6.2"
  }
}